do local Players=game:GetService("Players");local TweenService=game:GetService("TweenService");local UserInputService=game:GetService("UserInputService");local CoreGui=game:GetService("CoreGui");local TextService=game:GetService("TextService");local localPlayer=Players.LocalPlayer;local oldGui=CoreGui:FindFirstChild("UltimateFlingGui");if oldGui then oldGui:Destroy();end local currentLang="EN";local locales={EN={title="Ultimate Fling GUI",btnStart="START",btnStop="STOP",btnAll="SELECT ALL",btnUnselect="UNSELECT ALL",notifStarted="Fling started for <b>%s</b> target(s).",notifStopped="Fling has been stopped",notifNoTargets="No targets selected!",notifSitting="Player <b>%s</b> is sitting. Try again when the player is not seated.",notifNoParts="Player <b>%s</b> has no valid parts. Try again.",notifReady="Your character is not ready. Try again.",notifSelectAll="All targets selected.",notifUnselectAll="All targets unselected.",confirmTitle="Are you sure?",confirmYes="Yes",confirmNo="No"},RU={title="Ultimate Fling GUI",btnStart="СТАРТ",btnStop="СТОП",btnAll="ВЫБРАТЬ ВСЕХ",btnUnselect="СНЯТЬ ВЫДЕЛЕНИЕ",notifStarted="Флинг запущен на <b>%s</b> цел(ей).",notifStopped="Флинг остановлен.",notifNoTargets="Ни одна цель не выбрана!",notifSitting="Игрок <b>%s</b> сидит. Попробуйте снова, когда игрок не будет сидеть.",notifNoParts="У <b>%s</b> не найдены части тела. Попробуйте снова.",notifReady="Ваш персонаж не готов. Попробуйте снова.",notifSelectAll="Выбраны все цели.",notifUnselectAll="Выбор целей отменен.",confirmTitle="Вы уверены?",confirmYes="Да",confirmNo="Нет"}};local HEADER_HEIGHT=42;local BLUE=Color3.fromRGB(0,120,255);local GREEN=Color3.fromRGB(0,200,100);local RED=Color3.fromRGB(200,50,50);local BTN_NORMAL=Color3.fromRGB(45,45,50);local BTN_PRESS=Color3.fromRGB(30,30,36);local SelectedTargets={};local FlingActive=false;local isConfirming=false;getgenv().OldPos=nil;getgenv().FPDH=workspace.FallenPartsDestroyHeight;local screenGui=Instance.new("ScreenGui");screenGui.Name="UltimateFlingGui";screenGui.IgnoreGuiInset=true;screenGui.Parent=CoreGui;local mainFrame=Instance.new("Frame");mainFrame.Size=UDim2.new(0,250,0,350);mainFrame.Position=UDim2.new(0.05,0,0.4,0);mainFrame.BackgroundColor3=Color3.fromRGB(30,30,35);mainFrame.BorderSizePixel=0;mainFrame.Active=true;mainFrame.Parent=screenGui;Instance.new("UICorner",mainFrame).CornerRadius=UDim.new(0,8);local clipFrame=Instance.new("Frame");clipFrame.Size=UDim2.new(1,0,1, -HEADER_HEIGHT);clipFrame.Position=UDim2.new(0,0,0,HEADER_HEIGHT);clipFrame.BackgroundTransparency=1;clipFrame.ClipsDescendants=false;clipFrame.Parent=mainFrame;local dragHeader=Instance.new("Frame");dragHeader.Size=UDim2.new(1,0,0,40);dragHeader.BackgroundTransparency=1;dragHeader.ZIndex=5;dragHeader.Parent=mainFrame;local titleLabel=Instance.new("TextLabel");titleLabel.Position=UDim2.new(0,10,0,0);titleLabel.Size=UDim2.new(1, -110,1,0);titleLabel.BackgroundTransparency=1;titleLabel.Text=locales[currentLang].title;titleLabel.TextColor3=Color3.fromRGB(240,240,240);titleLabel.Font=Enum.Font.GothamBold;titleLabel.TextSize=15;titleLabel.TextXAlignment=Enum.TextXAlignment.Left;titleLabel.ZIndex=5;titleLabel.Parent=dragHeader;local line=Instance.new("Frame");line.Size=UDim2.new(1,0,0,2);line.Position=UDim2.new(0,0,0,40);line.BackgroundColor3=BLUE;line.BorderSizePixel=0;line.ZIndex=5;line.Parent=mainFrame;local toggleButton=Instance.new("TextButton");toggleButton.Size=UDim2.new(0,26,0,26);toggleButton.Position=UDim2.new(1, -65,0,7);toggleButton.BackgroundColor3=Color3.fromRGB(45,45,50);toggleButton.Text="−";toggleButton.TextColor3=Color3.fromRGB(240,240,240);toggleButton.Font=Enum.Font.GothamBold;toggleButton.TextSize=16;toggleButton.ZIndex=5;toggleButton.Parent=dragHeader;Instance.new("UICorner",toggleButton).CornerRadius=UDim.new(0,6);local closeButton=Instance.new("TextButton");closeButton.Size=UDim2.new(0,26,0,26);closeButton.Position=UDim2.new(1, -33,0,7);closeButton.BackgroundColor3=RED;closeButton.Text="×";closeButton.TextColor3=Color3.fromRGB(240,240,240);closeButton.Font=Enum.Font.GothamBold;closeButton.TextSize=14;closeButton.ZIndex=5;closeButton.Parent=dragHeader;Instance.new("UICorner",closeButton).CornerRadius=UDim.new(0,6);local langButton=Instance.new("TextButton");langButton.Size=UDim2.new(0,32,0,26);langButton.Position=UDim2.new(1, -102,0,7);langButton.BackgroundColor3=Color3.fromRGB(45,45,50);langButton.Text="EN";langButton.TextColor3=BLUE;langButton.Font=Enum.Font.GothamBold;langButton.TextSize=12;langButton.ZIndex=5;langButton.Parent=dragHeader;Instance.new("UICorner",langButton).CornerRadius=UDim.new(0,6);local controlPanel=Instance.new("Frame");controlPanel.Size=UDim2.new(1, -20,0,35);controlPanel.Position=UDim2.new(0,10,0,8);controlPanel.BackgroundTransparency=1;controlPanel.ZIndex=2;controlPanel.Parent=clipFrame;local function createControlBtn(text,size,pos,color) local btn=Instance.new("TextButton");btn.Size=size;btn.Position=pos;btn.BackgroundColor3=color;btn.Text=text;btn.TextColor3=Color3.fromRGB(255,255,255);btn.Font=Enum.Font.GothamBold;btn.TextSize=11;btn.TextScaled=true;btn.ZIndex=3;btn.Parent=controlPanel;Instance.new("UICorner",btn).CornerRadius=UDim.new(0,5);local uipadding=Instance.new("UIPadding");uipadding.PaddingLeft=UDim.new(0,4);uipadding.PaddingRight=UDim.new(0,4);uipadding.PaddingTop=UDim.new(0,4);uipadding.PaddingBottom=UDim.new(0,4);uipadding.Parent=btn;return btn;end local actionBtn=createControlBtn(locales[currentLang].btnStart,UDim2.new(0.35,0,1,0),UDim2.new(0,0,0,0),GREEN);local allBtn=createControlBtn(locales[currentLang].btnAll,UDim2.new(0.2,0,1,0),UDim2.new(0.38,0,0,0),BTN_NORMAL);local unselectBtn=createControlBtn(locales[currentLang].btnUnselect,UDim2.new(0.39,0,1,0),UDim2.new(0.61,0,0,0),BTN_NORMAL);local scrollingFrame=Instance.new("ScrollingFrame");scrollingFrame.Size=UDim2.new(1,0,1, -60);scrollingFrame.Position=UDim2.new(0,0,0,52);scrollingFrame.BackgroundTransparency=1;scrollingFrame.BorderSizePixel=0;scrollingFrame.ScrollBarThickness=4;scrollingFrame.ScrollBarImageColor3=BLUE;scrollingFrame.ClipsDescendants=true;scrollingFrame.ZIndex=2;scrollingFrame.Parent=clipFrame;local sPadding=Instance.new("UIPadding");sPadding.PaddingLeft=UDim.new(0,10);sPadding.PaddingRight=UDim.new(0,10);sPadding.PaddingTop=UDim.new(0,4);sPadding.PaddingBottom=UDim.new(0,40);sPadding.Parent=scrollingFrame;local listLayout=Instance.new("UIListLayout");listLayout.Padding=UDim.new(0,6);listLayout.SortOrder=Enum.SortOrder.LayoutOrder;listLayout.Parent=scrollingFrame;local confirmOverlay=Instance.new("Frame");confirmOverlay.Size=UDim2.new(1,0,1,0);confirmOverlay.BackgroundColor3=Color3.fromRGB(22,22,26);confirmOverlay.BackgroundTransparency=1;confirmOverlay.BorderSizePixel=0;confirmOverlay.ZIndex=20;confirmOverlay.Visible=false;confirmOverlay.Parent=mainFrame;Instance.new("UICorner",confirmOverlay).CornerRadius=UDim.new(0,8);local confirmTitleLabel=Instance.new("TextLabel");confirmTitleLabel.Size=UDim2.new(1, -20,0,40);confirmTitleLabel.Position=UDim2.new(0,10,0,105);confirmTitleLabel.BackgroundTransparency=1;confirmTitleLabel.Text=locales[currentLang].confirmTitle;confirmTitleLabel.TextColor3=Color3.fromRGB(245,245,245);confirmTitleLabel.Font=Enum.Font.GothamBold;confirmTitleLabel.TextSize=16;confirmTitleLabel.ZIndex=21;confirmTitleLabel.Parent=confirmOverlay;local yesButton=Instance.new("TextButton");yesButton.Size=UDim2.new(0,95,0,32);yesButton.Position=UDim2.new(0.5, -105,0.5,10);yesButton.BackgroundColor3=Color3.fromRGB(210,60,60);yesButton.Text=locales[currentLang].confirmYes;yesButton.TextColor3=Color3.fromRGB(255,255,255);yesButton.Font=Enum.Font.GothamBold;yesButton.TextSize=13;yesButton.TextScaled=true;yesButton.ZIndex=21;yesButton.Parent=confirmOverlay;Instance.new("UICorner",yesButton).CornerRadius=UDim.new(0,5);local yPadding=Instance.new("UIPadding");yPadding.PaddingLeft=UDim.new(0,4);yPadding.PaddingRight=UDim.new(0,4);yPadding.PaddingTop=UDim.new(0,4);yPadding.PaddingBottom=UDim.new(0,4);yPadding.Parent=yesButton;local noButton=Instance.new("TextButton");noButton.Size=UDim2.new(0,95,0,32);noButton.Position=UDim2.new(0.5,10,0.5,10);noButton.BackgroundColor3=Color3.fromRGB(45,45,50);noButton.Text=locales[currentLang].confirmNo;noButton.TextColor3=Color3.fromRGB(230,230,230);noButton.Font=Enum.Font.GothamBold;noButton.TextSize=13;noButton.TextScaled=true;noButton.ZIndex=21;noButton.Parent=confirmOverlay;Instance.new("UICorner",noButton).CornerRadius=UDim.new(0,5);local nPadding=Instance.new("UIPadding");nPadding.PaddingLeft=UDim.new(0,4);nPadding.PaddingRight=UDim.new(0,4);nPadding.PaddingTop=UDim.new(0,4);nPadding.PaddingBottom=UDim.new(0,4);nPadding.Parent=noButton;local activeNotifications={};local NOTIF_WIDTH=250;local NOTIF_HEIGHT=60;local NOTIF_SPACING=5;local SCREEN_PADDING=10;local function rearrangeNotifications() local currentYOffset= -SCREEN_PADDING;local tInfo=TweenInfo.new(0.2,Enum.EasingStyle.Quad,Enum.EasingDirection.Out);for _,notif in ipairs(activeNotifications) do local targetPos=UDim2.new(1, -NOTIF_WIDTH-SCREEN_PADDING ,1,currentYOffset-NOTIF_HEIGHT );TweenService:Create(notif,tInfo,{Position=targetPos}):Play();currentYOffset=currentYOffset-(NOTIF_HEIGHT + NOTIF_SPACING) ;end end local function removeNotification(notifFrame,textLabel,sideLine) for i=1, #activeNotifications do if (activeNotifications[i]==notifFrame) then table.remove(activeNotifications,i);break;end end if (notifFrame and notifFrame.Parent) then local tween=TweenService:Create(notifFrame,TweenInfo.new(0.2,Enum.EasingStyle.Quad,Enum.EasingDirection.In),{Position=UDim2.new(1.2,0,notifFrame.Position.Y.Scale,notifFrame.Position.Y.Offset),BackgroundTransparency=1});if textLabel then TweenService:Create(textLabel,TweenInfo.new(0.1),{TextTransparency=1}):Play();end if sideLine then TweenService:Create(sideLine,TweenInfo.new(0.1),{BackgroundTransparency=1}):Play();end tween:Play();tween.Completed:Connect(function() notifFrame:Destroy();end);rearrangeNotifications();end end local function showNotification(text,color) local notifFrame=Instance.new("Frame");notifFrame.Size=UDim2.new(0,NOTIF_WIDTH,0,NOTIF_HEIGHT);notifFrame.Position=UDim2.new(1.2,0,1, -NOTIF_HEIGHT-SCREEN_PADDING );notifFrame.BackgroundColor3=Color3.fromRGB(25,25,30);notifFrame.BorderSizePixel=0;notifFrame.Parent=screenGui;Instance.new("UICorner",notifFrame).CornerRadius=UDim.new(0,6);local sideLine=Instance.new("Frame");sideLine.Name="SideLine";sideLine.Size=UDim2.new(0,4,1,0);sideLine.BackgroundColor3=color or BLUE ;sideLine.BorderSizePixel=0;sideLine.Parent=notifFrame;Instance.new("UICorner",sideLine).CornerRadius=UDim.new(0,4);local textLabel=Instance.new("TextLabel");textLabel.Size=UDim2.new(1, -20,1,0);textLabel.Position=UDim2.new(0,12,0,0);textLabel.BackgroundTransparency=1;textLabel.Text=text;textLabel.RichText=true;textLabel.TextColor3=Color3.fromRGB(240,240,240);textLabel.Font=Enum.Font.GothamMedium;textLabel.TextSize=13;textLabel.TextWrapped=true;textLabel.TextXAlignment=Enum.TextXAlignment.Left;textLabel.Parent=notifFrame;table.insert(activeNotifications,1,notifFrame);rearrangeNotifications();task.delay(3.5,function() if (notifFrame and notifFrame.Parent) then removeNotification(notifFrame,textLabel,sideLine);end end);end local function updateHeaderStretch() local text=locales[currentLang].title;local baseBounds=TextService:GetTextSize(text,15,Enum.Font.GothamBold,Vector2.new(1000,40));local currentWidth=math.max(250,baseBounds.X + 130 );mainFrame.Size=UDim2.new(0,currentWidth,mainFrame.Size.Y.Scale,mainFrame.Size.Y.Offset);titleLabel.Size=UDim2.new(1, -110,1,0);end local dragging,dragInput,dragStart,startPos;local function update(input) local delta=input.Position-dragStart ;mainFrame.Position=UDim2.new(startPos.X.Scale,startPos.X.Offset + delta.X ,startPos.Y.Scale,startPos.Y.Offset + delta.Y );end local dragBeganConn=dragHeader.InputBegan:Connect(function(input) if isConfirming then return;end if ((input.UserInputType==Enum.UserInputType.MouseButton1) or (input.UserInputType==Enum.UserInputType.Touch)) then dragging=true;dragStart=input.Position;startPos=mainFrame.Position;local moveConn;moveConn=input.Changed:Connect(function() if (input.UserInputState==Enum.UserInputState.End) then dragging=false;if moveConn then moveConn:Disconnect();end end end);end end);local dragChangedConn=dragHeader.InputChanged:Connect(function(input) if isConfirming then return;end if ((input.UserInputType==Enum.UserInputType.MouseMovement) or (input.UserInputType==Enum.UserInputType.Touch)) then dragInput=input;end end);local uiChangedConn=UserInputService.InputChanged:Connect(function(input) if ((input==dragInput) and dragging) then update(input);end end);local function SkidFling(TargetPlayer) local Character=localPlayer.Character;local Humanoid=Character and Character:FindFirstChildOfClass("Humanoid") ;local RootPart=Humanoid and Humanoid.RootPart ;local TCharacter=TargetPlayer.Character;if  not TCharacter then return;end local THumanoid=TCharacter:FindFirstChildOfClass("Humanoid");local TRootPart=THumanoid and THumanoid.RootPart ;local THead=TCharacter:FindFirstChild("Head");local Accessory=TCharacter:FindFirstChildOfClass("Accessory");local Handle=Accessory and Accessory:FindFirstChild("Handle") ;if (Character and Humanoid and RootPart) then if (RootPart.Velocity.Magnitude<50) then getgenv().OldPos=RootPart.CFrame;end if (THumanoid and THumanoid.Sit) then return showNotification(string.format(locales[currentLang].notifSitting,TargetPlayer.Name),RED);end if THead then workspace.CurrentCamera.CameraSubject=THead;elseif Handle then workspace.CurrentCamera.CameraSubject=Handle;elseif (THumanoid and TRootPart) then workspace.CurrentCamera.CameraSubject=THumanoid;end if  not TCharacter:FindFirstChildWhichIsA("BasePart") then return;end local FPos=function(BasePart,Pos,Ang) RootPart.CFrame=CFrame.new(BasePart.Position) * Pos * Ang ;Character:SetPrimaryPartCFrame(CFrame.new(BasePart.Position) * Pos * Ang );RootPart.Velocity=Vector3.new(90000000,90000000 * 10 ,90000000);RootPart.RotVelocity=Vector3.new(900000000,900000000,900000000);end;local SFBasePart=function(BasePart) local TimeToWait=2;local Time=tick();local Angle=0;repeat if (RootPart and THumanoid) then if (BasePart.Velocity.Magnitude<50) then Angle=Angle + 100 ;FPos(BasePart,CFrame.new(0,1.5,0) + ((THumanoid.MoveDirection * BasePart.Velocity.Magnitude)/1.25) ,CFrame.Angles(math.rad(Angle),0,0));task.wait();FPos(BasePart,CFrame.new(0, -1.5,0) + ((THumanoid.MoveDirection * BasePart.Velocity.Magnitude)/1.25) ,CFrame.Angles(math.rad(Angle),0,0));task.wait();else FPos(BasePart,CFrame.new(0,1.5,THumanoid.WalkSpeed),CFrame.Angles(math.rad(90),0,0));task.wait();FPos(BasePart,CFrame.new(0, -1.5, -THumanoid.WalkSpeed),CFrame.Angles(0,0,0));task.wait();end end until ((Time + TimeToWait)<tick()) or  not FlingActive  end;workspace.FallenPartsDestroyHeight=NaN;local BV=Instance.new("BodyVelocity");BV.Parent=RootPart;BV.Velocity=Vector3.new(0,0,0);BV.MaxForce=Vector3.new(8999999488,8999999488,8999999488);Humanoid:SetStateEnabled(Enum.HumanoidStateType.Seated,false);if TRootPart then SFBasePart(TRootPart);elseif THead then SFBasePart(THead);elseif Handle then SFBasePart(Handle);else return showNotification(string.format(locales[currentLang].notifNoParts,TargetPlayer.Name),RED);end BV:Destroy();Humanoid:SetStateEnabled(Enum.HumanoidStateType.Seated,true);workspace.CurrentCamera.CameraSubject=Humanoid;if getgenv().OldPos then repeat RootPart.CFrame=getgenv().OldPos * CFrame.new(0,0.5,0) ;Character:SetPrimaryPartCFrame(getgenv().OldPos * CFrame.new(0,0.5,0) );Humanoid:ChangeState("GettingUp");for _,part in pairs(Character:GetChildren()) do if part:IsA("BasePart") then part.Velocity,part.RotVelocity=Vector3.new(),Vector3.new();end end task.wait();until (RootPart.Position-getgenv().OldPos.p).Magnitude<25  workspace.FallenPartsDestroyHeight=getgenv().FPDH;end else showNotification(locales[currentLang].notifReady,RED);end end local function countTargets() local c=0;for _ in pairs(SelectedTargets) do c=c + 1 ;end return c;end local playButtonsConnections={};local SCROLL_DRAG_THRESHOLD=6;local function updatePlayerList() for _,conn in ipairs(playButtonsConnections) do conn:Disconnect();end table.clear(playButtonsConnections);for _,child in ipairs(scrollingFrame:GetChildren()) do if child:IsA("TextButton") then child:Destroy();end end for _,player in ipairs(Players:GetPlayers()) do if (player==localPlayer) then continue;end local displayName=player.DisplayName;local hasDisplay=displayName~=player.Name ;local btnHeight=(hasDisplay and 52) or 35 ;local playBtn=Instance.new("TextButton");playBtn.Size=UDim2.new(1, -4,0,btnHeight);playBtn.BackgroundColor3=BTN_NORMAL;playBtn.BorderSizePixel=0;playBtn.Text="";playBtn.AutoButtonColor=false;playBtn.ZIndex=3;playBtn.Parent=scrollingFrame;Instance.new("UICorner",playBtn).CornerRadius=UDim.new(0,6);local pStroke=Instance.new("UIStroke");pStroke.Color=(SelectedTargets[player.Name] and GREEN) or BLUE ;pStroke.Thickness=(SelectedTargets[player.Name] and 2.5) or 1.5 ;pStroke.ApplyStrokeMode=Enum.ApplyStrokeMode.Border;pStroke.Parent=playBtn;local nameLabel=Instance.new("TextLabel");nameLabel.Size=UDim2.new(1, -12,0,20);nameLabel.Position=UDim2.new(0,10,0,8);nameLabel.BackgroundTransparency=1;nameLabel.Text=player.Name;nameLabel.TextColor3=Color3.fromRGB(240,240,240);nameLabel.Font=Enum.Font.GothamBold;nameLabel.TextSize=14;nameLabel.TextXAlignment=Enum.TextXAlignment.Left;nameLabel.ZIndex=4;nameLabel.Parent=playBtn;if hasDisplay then local dispLabel=Instance.new("TextLabel");dispLabel.Size=UDim2.new(1, -12,0,16);dispLabel.Position=UDim2.new(0,10,0,28);dispLabel.BackgroundTransparency=1;dispLabel.Text="("   .. displayName   .. ")" ;dispLabel.TextColor3=Color3.fromRGB(130,130,140);dispLabel.Font=Enum.Font.GothamMedium;dispLabel.TextSize=11;dispLabel.TextXAlignment=Enum.TextXAlignment.Left;dispLabel.ZIndex=4;dispLabel.Parent=playBtn;end local downPos=nil;local isScrolling=false;local pressDown=playBtn.MouseButton1Down:Connect(function(x,y) if isConfirming then return;end downPos=Vector2.new(x,y);isScrolling=false;TweenService:Create(playBtn,TweenInfo.new(0.08,Enum.EasingStyle.Quad),{BackgroundColor3=BTN_PRESS}):Play();end);local pressUp=playBtn.MouseButton1Up:Connect(function() if isConfirming then return;end TweenService:Create(playBtn,TweenInfo.new(0.15,Enum.EasingStyle.Quad),{BackgroundColor3=BTN_NORMAL}):Play();end);local moveConn=UserInputService.InputChanged:Connect(function(input) if isConfirming then return;end if (downPos and ((input.UserInputType==Enum.UserInputType.MouseMovement) or (input.UserInputType==Enum.UserInputType.Touch))) then local delta=Vector2.new(input.Position.X,input.Position.Y) -downPos ;if (delta.Magnitude>SCROLL_DRAG_THRESHOLD) then isScrolling=true;end end end);local clickConn=playBtn.MouseButton1Click:Connect(function() if (isConfirming or isScrolling) then return;end if SelectedTargets[player.Name] then SelectedTargets[player.Name]=nil;pStroke.Color=BLUE;pStroke.Thickness=1.5;else SelectedTargets[player.Name]=player;pStroke.Color=GREEN;pStroke.Thickness=2.5;end end);table.insert(playButtonsConnections,pressDown);table.insert(playButtonsConnections,pressUp);table.insert(playButtonsConnections,moveConn);table.insert(playButtonsConnections,clickConn);end scrollingFrame.CanvasSize=UDim2.new(0,0,0,listLayout.AbsoluteContentSize.Y + sPadding.PaddingBottom.Offset );end local function updateActionBtnDesign() if FlingActive then actionBtn.Text=locales[currentLang].btnStop;actionBtn.BackgroundColor3=RED;else actionBtn.Text=locales[currentLang].btnStart;actionBtn.BackgroundColor3=GREEN;end end actionBtn.MouseButton1Click:Connect(function() if isConfirming then return;end if FlingActive then FlingActive=false;updateActionBtnDesign();showNotification(locales[currentLang].notifStopped,BLUE);else local count=countTargets();if (count==0) then return showNotification(locales[currentLang].notifNoTargets,RED);end FlingActive=true;updateActionBtnDesign();showNotification(string.format(locales[currentLang].notifStarted,tostring(count)),GREEN);task.spawn(function() while FlingActive do local validTargets={};for name,player in pairs(SelectedTargets) do if (player and player.Parent) then validTargets[name]=player;else SelectedTargets[name]=nil;end end for _,player in pairs(validTargets) do if FlingActive then SkidFling(player);task.wait(0.1);else break;end end task.wait(0.5);end FlingActive=false;updateActionBtnDesign();end);end end);allBtn.MouseButton1Click:Connect(function() if isConfirming then return;end local addedAny=false;for _,p in ipairs(Players:GetPlayers()) do if (p~=localPlayer) then SelectedTargets[p.Name]=p;addedAny=true;end end updatePlayerList();if addedAny then showNotification(locales[currentLang].notifSelectAll,GREEN);end end);unselectBtn.MouseButton1Click:Connect(function() if isConfirming then return;end if (countTargets()>0) then table.clear(SelectedTargets);updatePlayerList();showNotification(locales[currentLang].notifUnselectAll,BLUE);end end);local isCollapsed=false;local originalHeight=350;local wasCollapsedBeforeClose=false;local toggleConn=toggleButton.MouseButton1Click:Connect(function() if isConfirming then return;end isCollapsed= not isCollapsed;if isCollapsed then clipFrame.Visible=false;line.Visible=false;toggleButton.Text="+";TweenService:Create(mainFrame,TweenInfo.new(0.2,Enum.EasingStyle.Quad),{Size=UDim2.new(0,mainFrame.Size.X.Offset,0,40)}):Play();else local tween=TweenService:Create(mainFrame,TweenInfo.new(0.2,Enum.EasingStyle.Quad),{Size=UDim2.new(0,mainFrame.Size.X.Offset,0,originalHeight)});tween:Play();tween.Completed:Connect(function() if  not isCollapsed then clipFrame.Visible=true;line.Visible=true;end end);toggleButton.Text="−";end end);local langConn=langButton.MouseButton1Click:Connect(function() if isConfirming then return;end currentLang=((currentLang=="EN") and "RU") or "EN" ;langButton.Text=currentLang;titleLabel.Text=locales[currentLang].title;confirmTitleLabel.Text=locales[currentLang].confirmTitle;yesButton.Text=locales[currentLang].confirmYes;noButton.Text=locales[currentLang].confirmNo;allBtn.Text=locales[currentLang].btnAll;unselectBtn.Text=locales[currentLang].btnUnselect;updateHeaderStretch();updateActionBtnDesign();updatePlayerList();end);local function showConfirmOverlay() confirmOverlay.Visible=true;confirmOverlay.BackgroundTransparency=1;for _,child in ipairs(confirmOverlay:GetDescendants()) do if (child:IsA("TextLabel") or child:IsA("TextButton")) then child.TextTransparency=1;TweenService:Create(child,TweenInfo.new(0.2),{TextTransparency=0}):Play();end end TweenService:Create(confirmOverlay,TweenInfo.new(0.2,Enum.EasingStyle.Quad),{BackgroundTransparency=0.05}):Play();end local function hideConfirmOverlay(callback) TweenService:Create(confirmOverlay,TweenInfo.new(0.15,Enum.EasingStyle.Quad),{BackgroundTransparency=1}):Play();for _,child in ipairs(confirmOverlay:GetDescendants()) do if (child:IsA("TextLabel") or child:IsA("TextButton")) then TweenService:Create(child,TweenInfo.new(0.12),{TextTransparency=1}):Play();end end task.delay(0.15,function() confirmOverlay.Visible=false;if callback then callback();end end);end local function closeGuiWithAnimation() FlingActive=false;dragBeganConn:Disconnect();dragChangedConn:Disconnect();uiChangedConn:Disconnect();toggleConn:Disconnect();langConn:Disconnect();for _,conn in ipairs(playButtonsConnections) do conn:Disconnect();end for _,child in ipairs(mainFrame:GetDescendants()) do if (child:IsA("TextLabel") or child:IsA("TextButton")) then TweenService:Create(child,TweenInfo.new(0.15),{TextTransparency=1}):Play();elseif child:IsA("Frame") then TweenService:Create(child,TweenInfo.new(0.15),{BackgroundTransparency=1}):Play();end end local t1=TweenService:Create(mainFrame,TweenInfo.new(0.25,Enum.EasingStyle.Back,Enum.EasingDirection.In),{Size=UDim2.new(0,mainFrame.Size.X.Offset,0,0),BackgroundTransparency=1});t1:Play();t1.Completed:Connect(function() screenGui:Destroy();end);end local closeConn=closeButton.MouseButton1Click:Connect(function() if isConfirming then return;end isConfirming=true;wasCollapsedBeforeClose=isCollapsed;if isCollapsed then isCollapsed=false;toggleButton.Text="−";clipFrame.Visible=true;line.Visible=true;local expandTween=TweenService:Create(mainFrame,TweenInfo.new(0.2,Enum.EasingStyle.Quad),{Size=UDim2.new(0,mainFrame.Size.X.Offset,0,originalHeight)});expandTween:Play();expandTween.Completed:Connect(showConfirmOverlay);else showConfirmOverlay();end end);local yesConn=yesButton.MouseButton1Click:Connect(closeGuiWithAnimation);local noConn=noButton.MouseButton1Click:Connect(function() hideConfirmOverlay(function() isConfirming=false;if wasCollapsedBeforeClose then isCollapsed=true;toggleButton.Text="+";clipFrame.Visible=false;line.Visible=false;TweenService:Create(mainFrame,TweenInfo.new(0.2,Enum.EasingStyle.Quad),{Size=UDim2.new(0,mainFrame.Size.X.Offset,0,40)}):Play();end end);end);yesButton.MouseEnter:Connect(function() TweenService:Create(yesButton,TweenInfo.new(0.15),{BackgroundColor3=Color3.fromRGB(240,70,70)}):Play();end);yesButton.MouseLeave:Connect(function() TweenService:Create(yesButton,TweenInfo.new(0.15),{BackgroundColor3=Color3.fromRGB(210,60,60)}):Play();end);noButton.MouseEnter:Connect(function() TweenService:Create(noButton,TweenInfo.new(0.15),{BackgroundColor3=Color3.fromRGB(60,60,65)}):Play();end);noButton.MouseLeave:Connect(function() TweenService:Create(noButton,TweenInfo.new(0.15),{BackgroundColor3=Color3.fromRGB(45,45,50)}):Play();end);updateHeaderStretch();updatePlayerList();local pAdded=Players.PlayerAdded:Connect(updatePlayerList);local pRemoved=Players.PlayerRemoving:Connect(function(player) SelectedTargets[player.Name]=nil;updatePlayerList();end);local destroyingConn;destroyingConn=screenGui.Destroying:Connect(function() pAdded:Disconnect();pRemoved:Disconnect();closeConn:Disconnect();yesConn:Disconnect();noConn:Disconnect();destroyingConn:Disconnect();FlingActive=false;end); end
